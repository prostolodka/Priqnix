# Changelog — 2026-06-04 (час 3)

## Сделано

### 1. Hilt Dependency Injection
- **Добавлен плагин**: `com.google.dagger.hilt.android` (v2.50)
- **Новый файл**: `di/DatabaseModule.kt` — модуль предоставления зависимостей
- **Новый файл**: `PriqnixApplication.kt` — `@HiltAndroidApp`
- **Обновлено**: `MainActivity.kt` — `@AndroidEntryPoint`, поля `@Inject`
- **Обновлено**: `InfoViewModel`, `EmployeesViewModel` — `@HiltViewModel`, `@Inject constructor`
- **Удалены**: ручные фабрики `InfoViewModelFactory`, `EmployeesViewModelFactory`
- **Обновлён**: `DatabaseInitializer` — `@Inject constructor`

### 2. Переключатель тёмной/светлой темы
- **Изменено**: `PriqnixTheme.kt` — параметр `darkTheme: Boolean` (было `= true`)
- **Изменено**: `MainActivity.kt` — `remember { mutableStateOf(true) }` для темы
- **Изменено**: `AboutScreen.kt` — кнопка `DarkMode` в тулбаре, вызывающая `onThemeToggle`

### 3. Настройка подписи APK (release)
- **Создан**: `app/release.keystore` (keyAlias: priqnix)
- **Добавлен**: `signingConfigs.release` в `app/build.gradle`
- **Добавлен**: `buildTypes.release` с signingConfig

### 4. Splash Screen (Android 12+)
- **Добавлена зависимость**: `core-splashscreen:1.0.1`
- **Изменён**: `themes.xml` — добавлен `Theme.Priqnix.Splash`
- **Изменён**: `AndroidManifest.xml` — `android:theme="@style/Theme.Priqnix.Splash"`
- **Изменён**: `MainActivity.kt` — `installSplashScreen()`

## Сборка
- `assembleDebug` — **BUILD SUCCESSFUL in 43s, 0 warnings**
- `assembleRelease` — **BUILD SUCCESSFUL in 2m 18s**
- Debug APK: `app/build/outputs/apk/debug/app-debug.apk`
- Release APK: `app/build/outputs/apk/release/app-release.apk`
