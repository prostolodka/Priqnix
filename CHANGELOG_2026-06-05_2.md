# Changelog — 2026-06-05 (час 2)

## Новые фичи
- **FAQ** — экран с категориями, поиском, expandable карточками
- **Избранное** — Room-таблица `favorites`, DAO, ViewModel, экран, кнопка ♡ на карточках
- **Смена темы** — `ThemeManager` через SharedPreferences, кнопка на главном экране
- **CI/CD** — GitHub Actions workflow (`.github/workflows/android.yml`)

## Исправления (по замечаниям)
1. **Room миграция** — `fallbackToDestructiveMigration()` → `MIGRATION_1_2` + `MIGRATION_2_3`
2. **`.gitignore`** — добавлен `app/release.keystore`, удалён из git-трекинга
3. **AboutScreen** — убрана кнопка темы, роут добавлен в TopMenuBar на главной
4. **HomeScreen цвета** — `Color(0xFF2A2A35)` → `MaterialTheme.colorScheme.surfaceVariant`, `Color.White` → `onBackground`/`onSurface`
5. **Поиск** — `InfoViewModel` теперь сохраняет `searchQuery` через `SavedStateHandle` (переживает поворот)
6. **AddItemDialog** — валидация с красной подсветкой пустых полей
7. **Загрузка БД** — сплэш-экран ждёт инициализации, показан `CircularProgressIndicator` пока БД не готова
8. **Навигация** — TopMenuBar на главной кликабельный, совпадает с нижним меню + "О компании"
